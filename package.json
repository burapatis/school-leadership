{
  "name": "school-leadership",
  "private": true,
  "scripts": {
    "build": "hugo --minify && pagefind --site public",
    "build:pages": "hugo --minify --baseURL \"https://${GITHUB_REPOSITORY_OWNER:-example}.github.io/school-leadership/\" && pagefind --site public",
    "index": "pagefind --site public",
    "dev": "hugo server -D"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
